diff --git a/main/xiaozhi-server/core/http_server.py b/main/xiaozhi-server/core/http_server.py
index feb96f3..36b7775 100644
--- a/main/xiaozhi-server/core/http_server.py
+++ b/main/xiaozhi-server/core/http_server.py
@@ -3,6 +3,7 @@ from aiohttp import web
 from config.logger import setup_logging
 from core.api.ota_handler import OTAHandler
 from core.api.vision_handler import VisionHandler
+from core.api.aivmt_handler import AivmtHandler
 
 TAG = __name__
 
@@ -13,6 +14,7 @@ class SimpleHttpServer:
         self.logger = setup_logging()
         self.ota_handler = OTAHandler(config)
         self.vision_handler = VisionHandler(config)
+        self.aivmt_handler = AivmtHandler(config)
 
     def _get_websocket_url(self, local_ip: str, port: int) -> str:
         """获取websocket地址
@@ -62,6 +64,17 @@ class SimpleHttpServer:
                             ),
                         ]
                     )
+                # AIVMT: receive finished standardized-patient encounters (local archive)
+                app.add_routes(
+                    [
+                        web.post(
+                            "/aivmt/encounter", self.aivmt_handler.handle_post
+                        ),
+                        web.options(
+                            "/aivmt/encounter", self.aivmt_handler.handle_options
+                        ),
+                    ]
+                )
                 # 添加路由
                 app.add_routes(
                     [
