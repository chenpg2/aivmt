"""PatientEngine — the AI standardized patient (plain-text chat with a persona).

NOTE: the patient "actor" model is operational tooling and is NOT the scored entity;
the scoring model is governed by the pre-registration (§8). Any